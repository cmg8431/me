import { styled } from '@/stitches.config';

export const DefaultLayoutContainer = styled('div', {
  width: '100%',
  height: '100%',
  padding: '0rem 2rem',
  marginLeft: 'auto',
  marginRight: 'auto',
  maxWidth: '700px',
  overflowY: 'auto',
});
