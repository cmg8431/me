import { styled } from '@/stitches.config';

export const DefaultLayoutContainer = styled('div', {
  width: '100%',
  height: '100%',
  padding: '0rem 1rem',
  marginLeft: 'auto',
  marginRight: 'auto',
  maxWidth: '750px',
  overflowY: 'auto',
});
